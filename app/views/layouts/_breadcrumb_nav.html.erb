<div class="inner"> 
<% if account_signed_in? %>
<%= link_to "Cerrar sesion:  ", destroy_account_session_path,method: :delete , class: "pull-right m-l-10"%>
<% else %>
<%= link_to "Mi Cuenta:  ", root_path , class: "pull-right m-l-10"%>
<% end %>

<%= link_to "Nuevo Anuncio   ", new_post_path, class: "pull-right m-l-10"%>
<%= link_to "Inicio", root_path %> >>
<% if @post.present? && !@post.new_record?
category = @post.category
parent = @post.category.parent


%>

<% if parent.present? %>
<%= link_to parent.name, category_path(parent) %> >>

<% end %>


<%= link_to @post.category.name, category_path(@post.category) %> >>
<%= @post.title %>

<% elsif @category.present? 
   parent = @category.parent  %>

   <%if parent.present? %>

    <%= link_to parent.name, category_path(parent) %> >>
    <% end %>
   <%= @category.name %>


<% end %>

</div>